import React from 'react';
import { Typography } from '@mui/material';

const About: React.FC = () => {
    return (
        <div>
            <Typography variant="h1">About Page</Typography>
            <Typography>Learn more about us on this page.</Typography>
        </div>
    );
}

export default About;